const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    colors: {
      gray: colors.coolGray,
      primary: "#383856",
      red: colors.rose,
      pink: colors.fuchsia,
    },
    fontFamily: {
      sans: ['Lora', 'serif'],
    },
  },
}
