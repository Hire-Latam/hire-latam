const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    colors: {
      gray: colors.coolGray,
      primary: "#003049",
      red: colors.rose,
      pink: colors.fuchsia,
    },
    fontFamily: {
      sans: ['Poppins', 'serif'],
    },
  },
}
